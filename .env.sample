# Production endpoint (recommended)
AUDIO_WS_URL=wss://your-server.example/ws/audio
# AUDIO_WS_PASSWORD=replace-with-shared-password

# Local binary audio server example:
# AUDIO_WS_URL=ws://127.0.0.1:8000/ws/audio

# Legacy local echo test server example:
# AUDIO_WS_URL=ws://127.0.0.1:8765
